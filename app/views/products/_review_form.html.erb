write your own review

<%= form_with model: Review.new do |review_form| %>
  <%= review_form.text_area :content, cols: 40, rows: 10 %>
  <%= review_form.hidden_field :product, value: @product.id %><br>
  <%= review_form.submit %>
<% end %>