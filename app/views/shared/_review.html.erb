<%
    # TODO: If staff, show IP address
    # TODO: Implement DOMPurify to allow specific tags.
    # TODO: Talking point on potential concerns around JavaScript sanitizer on Rails-generated content. I.e., rendered in page prior to sanitization and could have attribute injection

%>
Reviewed By <%= review.user.email %> on <%= review.created_at %>
<br>
<%= review.content.html_safe %>